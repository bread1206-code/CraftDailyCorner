@* @model CraftDailyCorner.ViewModels.Front.VMCheckout

@{
    ViewData["Title"] = "確認訂單";
}

<div class="container my-5">

    <h3 class="mb-4">🧾 確認訂單內容</h3>

    <!-- 商品清單 -->
    <div class="card mb-4">
        <div class="card-body">

            <table class="table align-middle">
                <thead class="table-light">
                    <tr>
                        <th>商品名稱</th>
                        <th class="text-end">單價</th>
                        <th class="text-center">數量</th>
                        <th class="text-end">小計</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model.Items)
                    {
                        <tr>
                            <td>@item.ProductName</td>
                            <td class="text-end">
                                @item.Price.ToString("N0")
                            </td>
                            <td class="text-center">
                                @item.Quantity
                            </td>
                            <td class="text-end">
                                @(((int)Math.Floor(item.Price * item.Quantity)).ToString("N0"))
                            </td>
                        </tr>
                    }
                </tbody>
            </table>

        </div>
    </div>

    <!-- 總金額 -->
    <div class="card mb-4">
        <div class="card-body d-flex justify-content-between align-items-center">
            <span class="fs-5 fw-bold">訂單總金額</span>
            <span class="fs-4 text-danger fw-bold">
                NT$ @Model.TotalAmount.ToString("N0")
            </span>
        </div>
    </div>
    <form asp-action="CheckoutConfirm"
          asp-controller="Order"
          method="post">

        @Html.AntiForgeryToken()

        <!-- 收件人資料 -->
        <div class="card mb-4">
            <div class="card-body">
                <h5 class="mb-3"><i class="bi bi-box-seam"> </i>收件人資訊</h5>

                <div class="mb-3">
                    <label asp-for="ReceiverName" class="form-label"></label>
                    <input asp-for="ReceiverName" class="form-control" />
                    <span asp-validation-for="ReceiverName" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="ReceiverPhone" class="form-label"></label>
                    <input asp-for="ReceiverPhone" class="form-control" />
                    <span asp-validation-for="ReceiverPhone" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="ReceiverAddress" class="form-label"></label>
                    <input asp-for="ReceiverAddress" class="form-control" />
                    <span asp-validation-for="ReceiverAddress" class="text-danger"></span>
                </div>
            </div>
        </div>

        <!-- 按鈕 -->
        <div class="d-flex justify-content-between">
            <a asp-controller="Home"
               asp-action="Index"
               class="btn btn-lg btn-outline-secondary">
                <i class="bi bi-arrow-left"> </i>返回首頁
            </a>

            <button type="submit"
                    class="btn btn-primary btn-lg"
                    onclick="this.disabled=true; this.form.submit();">
                確認送出訂單
            </button>
        </div>
    </form>
</div>
 *@